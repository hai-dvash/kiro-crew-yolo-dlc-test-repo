{
  "name": "rps3d",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "3D Rock-Paper-Scissors with Wii-style mouse-gesture throws (showcase)",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "0.160.1"
  },
  "devDependencies": {
    "@types/three": "0.160.0",
    "typescript": "5.4.5",
    "vite": "5.2.11",
    "vitest": "1.6.0"
  }
}
